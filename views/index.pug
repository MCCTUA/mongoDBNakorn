doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(http-equiv="X-UA-Compatible", content="IE=edge")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title Document
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
  body
    .container.my-5
      h1 รายชื่อหนัง / ซีรีย์ (#{total})
      p.text-secondary (คุณอยู่หน้าที่ #{page})
      form
        .input-group
          input.form-control(name="keyword" type="search" placeholder="ระบุคำค้นหา...")
          button.btn.btn-outline-secondary ค้นหา
      hr
      if movies.length == 0
        p.text-danger.text-cengter ไม่พบข้อมูล
      else
        table.table.table-striped
          thead
            tr
              th ชื่อ
              th ประเภท
              th ภาษา
              th วันที่ฉาย
          tbody
            each movie in movies
              tr
                td= movie.title
                td= movie.type
                td= movie.languages
                td= movie.released
      ul.pagination.justify-content-center.flex-wrap

        - let pageIndex = 1
        while pageIndex <= totalPage
          li.page-item(class=page == pageIndex ? 'active' : '')
            a.page-link(href=`?page=${pageIndex}${keywordInput ? `&keyword=${keywordInput}` : ''}`)= pageIndex++




